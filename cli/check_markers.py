"""Shared markers for factual-check annotations in notes."""

# Marker so we can find and strip old callouts on re-runs.
CALLOUT_MARKER = "<!-- anki-check -->"
BROKEN_LINK_MARKER = "<!-- anki-check-link -->"
